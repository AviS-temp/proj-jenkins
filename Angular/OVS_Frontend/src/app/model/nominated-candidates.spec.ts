import { NominatedCandidates } from './nominated-candidates';

describe('NominatedCandidates', () => {
  it('should create an instance', () => {
    expect(new NominatedCandidates()).toBeTruthy();
  });
});
