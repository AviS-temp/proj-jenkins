import { RegisteredSocietyVoters } from './registered-society-voters';

describe('RegisteredSocietyVoters', () => {
  it('should create an instance', () => {
    expect(new RegisteredSocietyVoters()).toBeTruthy();
  });
});
